The program had a flaw in mapping token offsets to spaCy tokens, which caused an index out-of-bounds error. The offsets provided by the tokenizer were being incorrectly accessed. The spaCy tokenization also might not always align for each sub-token, especially with BPE tokenization methods. To fix this, we need to correct the way we align spaCy tokens with sub-word tokens provided by the tokenizer.